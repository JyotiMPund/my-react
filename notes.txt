->CREATING BASIC HELLO WORLD JS=>
  -------------------------------------
  
CODE:
		<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Namste React</title>
  </head>
  <body>
    <div id="root">
      <script>
        const heading = document.createElement("h1");
        heading.innerHTML = "Hello world from javascript";

        const root = document.getElementById("root");
        root.appendChild(heading);
      </script>
    </div>
  </body>
</html>

-- React.createElement -is an object
-------------------------------------------------------------------------------------
CDN?
------
Content Delivery Network->These are the website where the react hosted ,
we can pull react from there into our projects.

bellow you can see how can you inject react in your project:

CODE:
		<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Namste React</title>
  </head>
  <body>
    <div id="root">
     
    </div>

    <script
      crossorigin
      src="https://unpkg.com/react@18/umd/react.development.js"
    ></script>
    <script
      crossorigin
      src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"
    ></script>
  </body>
</html>
-------------------------------------------------------------------------------------------------
->HELLO WORLD BASIC PROGRAM IN REACT:
CODE:
		<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Namste React</title>
  </head>
  <body>
    <div id="root"></div>

    <script
      crossorigin
      src="https://unpkg.com/react@18/umd/react.development.js"
    ></script>
    <script
      crossorigin
      src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"
    ></script>

    <script>
      const heading = React.createElement("h1",{ id: "heading" },"Hello world from react"); -->It's create an object of the h1 tag
      const root = ReactDOM.createRoot(document.getElementById("root"));
      root.render(heading);
    </script>
  </body>
</html>
---------------------------------------------------------------------------------------------------------------

->IN GIT IF YOU ARE GETTING BELOW ERROR
--remote: Permission to JyotiMPund/my-react.git denied to jyotiP123456789.
fatal: unable to access 'https://github.com/JyotiMPund/my-react.git/': The requested URL returned error: 403

use This command--> git config --global credential.useHttpPath true
-------------------------------------------------------------------------------------------------------------
NPM AND NPM INIT--> 
-it manages the packages
-its standard repository for all the packages
-use npm init to create package.json file
-package.json is configuration for npm
----------------------------------------------------------------------------------------------------------------
BUNDLER-->
-bundler helps to bundle the app and push it to the production

--create-react-app uses webpack and bable behind the scenes

PARCEL BUNDLER-->
-------------
command to install->
npm install -D parcel --> its install as a dev depandancy using -D

   "parcel": "^2.14.0"-- carate is symbole with this it can update the version of parcel in future

   using tilde it can do major updates
-------------------------------------------------------------------------------------------------
PACKAGE.JSON(package.json):->

-its a configuaration for npm
-it keeps a track of what version of that package is installed into our system
-it keeps a approx version of the package
--------------------------------------------------------------------------------------------
PACKAGE-LOCK.JSON(package-lock.json):->
-its keep a track of exact version of the package and locks it
--------------------------------------------------------------------------------------------
NODE_MODULES(node_modules):->
- it contains the data of the dependancies of the packages
-its a collection of dependancies
-if we have package.json and package-lock.json file then we can regenarate node_modules
------------------------------------------------------------------------------------------
TRANSITIVE DEPENDANCIES:->

-its dependancies of depandancy
--------------------------------------------------------------------------------------------
NPX PARCEL INDEX.HTML(npx parcel index.html)->
-index.html is the source file ..by using above command app hosts on the server 

-npx means it executes package , and npm means its installing package
-------------------------------------------------------------------------------------------
NPM INSTALL REACT(npm install react)->
-this is the command to install react --as a normal depandancy
npm install react-dom->
-this is the command to install react-dom
----------------------------------------------------------------------------------------------
<script type="module" src="./App.js"></script>-->
-without type module it treats script   as a browser script or normal js file
if we import React and ReactDOM without type module
-with module type it will consider as a module and will not show any error
-----------------------------------------------------------------------------------------
NOTE:Remove entry point from package.json it can give the error for build like in this i have removed App.js
-------------------------------------------------------------------------------------------------------------
#PARCEL->
-------------
-Dev Build
-Local surver
-HMR=Hot module replacement--means it refresh automaticaly
-parcel uses-File watching algorithm-written in c++
-caching-Faster builds
-it will do image optimization
-if we do production build it will minify our files
-it will do bundling
-it will compress the file
-consistent Hashing
-code spliting
-Differential bundling-support older browser
-it gives diagnostic
-it gives error handling
-it can helps to host on HTTPs server
-Tree shaking-it will remove unused code
-different build for dev and production
-Parcel transpile code into js engine understandable formate with the help of Babel-Babel is a js compiler
JSX=>React.createElement=>ReatElement-js object=>HTML(render)
NOTE-If you write code in multiple line then wrap it in () brackets so that Babel can understant
----------------------------------------------------------------------------------------------------
-USE FOLLOWING COMMANND-->

for production build-->npx parcel build index.html
----------------------------------------------------------------------------------------------------
YOU CAN ADD BELOW CODE IN package.json FOR SUPPORTING BROWSER VERSION:->

browsersList:[
  lastt 2 chrome version
]
----------------------------------------------------------------------------------------------------
-CREATING SCRIPTS->
"scripts": {
    "start": "parcel index.html",--->this is for dev-->now we run project using-->npm run start-->also you use->npm start
    "build":"parcel build index.html",--->This is for prod-->now we can using -->npm run build
    "test": "jest"
  },
  -------------------------------------------------------------------------------------------------------------------------------------
  JSX?-->
  -it is not HTML in JS
  -jsx is a html like syntax or like XML
----------------------------------------------------------------------------------------------------------------------------
COMPONENT COMPOSITION->

CODE-:
const Title = () => <h1 id="heading">This is a react Element</h1>;

//React Functional Component
const Heading1 = () => {
  return (
    <div>
      <Title />
      <h1 id="heading1">This a react component</h1>
    </div>
  );
};

--Component composition means component inside component
------------------------------------------------------------------------------------------------------------------------
CONFIG DRIVEN UI:
-The design and content don't need to be hardcoded in the source code; 
-rather, it is driven by configuration files,
-meaning the same codebase can be used for different user interfaces based on the configuration.
-------------------------------------------------------------------------------------------------------------------
KEY->
--always use key -->if you pass id as a key it will help to identify the new item so that only that iten can render.
--you can also use index but its not a best practice to use index as a key
---------------------------------------------------------------------------------------------------------------------
TWO TYPES OF EXPORT/IMPORT->

-Default Export/import:
--export default component;
--import component from "path"

-Named Export/Import:
--export const component
--import {component} from "path"
-----------------------------------------------------------------------------------------------------------
#REACT HOOKS->
(Normal Js Utility Functions)
-useState()-->Superpowerfull state variable in react
-useeffect()

NOTE:-Whenever a state variable changes react rerender the component
-----------------------------------------------------------------------------------------------------------
Reconciliation algorithm(React Fiber)

VERTUAL DOM->
Its a representation of an actual DOM
React is fast bcz its does efficiant DOM manupullation

---------------------------------------------------------------------------------------------------------------------
Two API Rendering approch->

1)Loads->API>Render
2)Loads->Render->API->Render---its better approch
----------------------------------------------------------------------------------------------------------------------
USEEFFECT HOOKS

Code:
useEffect(() => {
    console.log("useEffect rendered");
  }, []);

  useEffect contains callback functiona and dependancies
  --whatever called inside the useEffect it calls after the component render

  ----------------------------------------------------------------------------------------------------------------
  REACT FIBER|REACT RECONCILIATION:

-it means react finds the difference between actual dom and vertual dom and only do the required changes
-thats why react is fast
-------------------------------------------------------------------------------------------
How Optional Chaining Works:
?. allows you to safely access a property or method.

If a property or reference before the ?. is null or undefined, the expression will return undefined instead of throwing an error.